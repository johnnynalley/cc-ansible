# agents Playbooks

Owner area: Codex, Claude archive sync, OpenClaw, and Hermes services.

## Operating Notes

- Key vars: codex_*, claude_memory_sync_*, openclaw_*, hermes_shadow_*,
  hermes_memory_continuity_*, hermes_profile_memory_*,
  hermes_profile_skills_*, hermes_automation_*.
- Template owners: templates/openclaw and templates/hermes.
- Script owners: templates/openclaw managed helper scripts.
- Isolated non-model services use repo-managed sources under `scripts/agents/`.
- The modern production behavior bundle is rooted at
  `files/openclaw/workspace/`; it is reconstructed and audited source, not a
  legacy workspace clone. Mutable memories and project data remain separate.
- Keep playbook metadata headers and `playbooks/README.md` in sync when behavior changes.
- OpenClaw intentionally tracks the npm `latest` channel. Do not add an exact package-version variable: native OpenClaw updates must survive later Ansible convergence instead of being downgraded.
- The isolated runtime resolves the stable core plus the reviewed Codex,
  Discord, Lossless Claw, and Mem0 plugins under a credential-less ephemeral
  build account with lifecycle scripts disabled. It validates and atomically
  promotes one root-owned versioned release, then separates the `openclaw`
  Gateway from the `openclaw-codex` model executor. Resolved versions are
  rollback records, not update-policy pins.

## Playbooks

| Playbook | Hosts | Purpose | Validation |
| --- | --- | --- | --- |
| `claude-memory-sync.yml` | `nas_server, orchestrator` | Configure Claude memory sync to NAS. | `ansible-playbook playbooks/agents/claude-memory-sync.yml --syntax-check` |
| `codex-cli.yml` | `orchestrator` | Configure Codex CLI. | `ansible-playbook playbooks/agents/codex-cli.yml --syntax-check` |
| `codex-memory-sync.yml` | `nas_server, orchestrator` | Configure Codex memory sync to NAS. | `ansible-playbook playbooks/agents/codex-memory-sync.yml --syntax-check` |
| `hermes-shadow.yml` | `hermes_hosts` | Stage the isolated, boot-disabled Hermes runtime with three OS identities; seed profile-owned identity and operating contracts only when absent; retain root-owned Discord, automation, security, and tool-authority policy; install a signed root-managed offline Tirith scanner; validate native config as each service identity; and keep production delivery and schedules disabled by default. | `ansible-playbook playbooks/agents/hermes-shadow.yml --syntax-check` |
| `hermes-replacement-node-rehearsal.yml` | `rehearsal_controller`, disposable dynamic `hermes_hosts` member | Rebuild the credential-free production platform/bootstrap boundary in a fresh rootless Podman systemd container, prove all three profiles and updater wiring while Gateways remain stopped, and remove the accepted target. Use only the dedicated empty-production inventory. | `ansible-playbook -i inventory/hermes-replacement-rehearsal.ini playbooks/agents/hermes-replacement-node-rehearsal.yml --syntax-check` |
| `hermes-production-cutover.yml` | `hermes_hosts` | Transactionally stop and preserve OpenClaw, prove delivery state is drained, privately enroll three distinct Discord identities, promote Astra, Dubble, and Rigel, activate native silent Rigel scheduling and native updates, preserve Health, and automatically restore OpenClaw on any promotion failure; disabled by default. | `ansible-playbook playbooks/agents/hermes-production-cutover.yml --syntax-check` |
| `hermes-production-runtime.yml` | `hermes_hosts` | Back up and converge only the rebuildable Hermes platform support, official Discord dependency/readiness boundary, bounded native-updater systemd lifecycle, and configured production Gateways one at a time while OpenClaw remains offline and Health remains active. It does not deploy profile-authored native guidance; disabled by default. | `ansible-playbook playbooks/agents/hermes-production-runtime.yml --syntax-check` |
| `hermes-native-gateway-migration.yml` | `hermes_hosts` | Copy retained flat profile state into native named profiles, replace handwritten base units through Hermes's native system installer, preserve hardened drop-ins, start every configured consumer, and restore the prior units/state on failure; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-native-gateway-migration.yml --syntax-check` |
| `hermes-memory-continuity.yml` | `hermes_hosts` | Install the reviewed LCM context engine and official Mem0 extra, convert the stopped OpenClaw LCM database and approved Astra Mem0 scopes into separate Hermes stores, prove exact reconciliation and live recall, and restore prior state on failure; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-memory-continuity.yml --syntax-check` |
| `hermes-profile-memory-continuity.yml` | `hermes_hosts` | Audit or transactionally activate one reviewed non-Astra profile's isolated stable LCM and Mem0 v3 stores from exact OpenClaw agent scopes, with source hashes, local plus managed rollback, one changed-run native restart, recall validation, and fail-closed cross-profile boundaries. Rigel imports only its exact agent scopes; Dubble imports only route-proven public history and initializes an empty private Mem0 collection while unresolved history remains offline. | `ansible-playbook playbooks/agents/hermes-profile-memory-continuity.yml --syntax-check` |
| `hermes-mem0-native-upgrade.yml` | `hermes_hosts` | Back up the shared runtime, resolve newest production-stable Mem0/NLP packages, convert Astra's complete native v2 collection into a fresh v3 dense+BM25 collection with filter indexes, verify live recall, and restore the exact prior runtime on failure; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-mem0-native-upgrade.yml --syntax-check` |
| `hermes-memory-servers.yml` | `hermes_hosts` | Audit official stable Qdrant/Ollama releases and managed Ollama service drift. With exact approval, either apply a rollback-protected Ollama-only context correction without restarting Astra or snapshot Qdrant, stage a checksummed Ollama release, update both backing servers, prove exact data/model/schema continuity and Mem0 recall, and restore prior state on failure. | `ansible-playbook playbooks/agents/hermes-memory-servers.yml --syntax-check` |
| `hermes-lcm-native-features.yml` | `hermes_hosts` | Audit or transactionally enable Astra's stable local LCM semantic, proactive, temporal, and all-session recall paths with verified SQLite/NFS rollback and one changed-run Gateway restart. | `ansible-playbook playbooks/agents/hermes-lcm-native-features.yml --syntax-check` |
| `hermes-lcm-backfill.yml` | `hermes_hosts` | Audit or converge staggered, low-priority native LCM summary/chunk backfill timers that use only local Ollama and never restart a Gateway. | `ansible-playbook playbooks/agents/hermes-lcm-backfill.yml --syntax-check` |
| `hermes-automation.yml` | `hermes_hosts` | Back up and transactionally converge the rebuildable automation platform, retained collectors, calendar/feed timers, and profile backups while preserving profile-owned native schedules by default. Explicit `audit`, missing-only `seed`, and exact `restore` modes cover the reviewed 17 Astra, one Dubble, and one Rigel declarations; seven historical-only rows remain retained but inactive. OpenClaw stays offline and files/timers are restored on failure; disabled by default. | `ansible-playbook playbooks/agents/hermes-automation.yml --syntax-check` |
| `hermes-remote-access.yml` | `hermes_hosts, linux_hosts:!hermes_hosts` | Provision Astra's dedicated non-root SSH identity on every reachable managed Linux host, require critical collector hosts, persist partial coverage for offline-host follow-up, and never reuse owner/Ansible/dbc keys or grant sudo/Docker access; disabled by default. | `ansible-playbook playbooks/agents/hermes-remote-access.yml --syntax-check` |
| `hermes-docker-inventory.yml` | `hermes_hosts` | Promote Astra's inventory-derived Docker host manifest, live-container reports, and approval-gated managed updater trigger without Docker socket/group or general SSH; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-docker-inventory.yml --syntax-check` |
| `hermes-compose-admin.yml` | `hermes_hosts, docker_hosts` | Enroll a separate forced-command identity and promote Astra's typed, rollback-backed Compose plan/apply/remove tools without Docker group, socket, shell, host binds, inline secrets, or volume deletion; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-compose-admin.yml --syntax-check` |
| `hermes-host-admin.yml` | `hermes_hosts, linux_hosts` | Enroll Astra's dedicated forced-command host-maintenance identity and promote typed status, health, package-update, service-lifecycle, and guarded reboot operations without general shell, root SSH, Docker access, or unbounded privilege; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-host-admin.yml --syntax-check` |
| `hermes-fleet-admin.yml` | `hermes_hosts` | Promote Astra's owner-session-only typed inspection and administration of Astra, Dubble, and Rigel native profile/workspace state, with compare-and-swap mutations, off-host rollback, audit, target validation, immutable `/etc/hermes` policy separation, and no target-agent approval dependency; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-fleet-admin.yml --syntax-check` |
| `hermes-arr-api.yml` | `hermes_hosts` | Extract enrolled Arr credentials without exposing values, install a credential-isolated broker, and promote Astra's typed read/write API tools with consequence policy, exact service discovery, filtered Prowlarr indexer-schema lookup, bound-owner-turn secret-bearing indexer test/create/update without a redundant prompt, audit evidence, and rollback. Generic writes remain approval-gated. Broker refresh is a non-propagating Gateway dependency; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-arr-api.yml --syntax-check` |
| `hermes-dashboard.yml` | `hermes_hosts` | Converge Astra's authenticated native Hermes dashboard on the reviewed Tailscale-only endpoint with root-private credentials, rollback, hardening, and Gateway readiness checks; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-dashboard.yml --syntax-check` |
| `hermes-openclaw-dry-run.yml` | `hermes_hosts` | Run the pinned official importer against an ephemeral shape-only, no-secret, read-only source and target; retain only root-private structural evidence and leave all services unchanged; disabled by default. | `ansible-playbook playbooks/agents/hermes-openclaw-dry-run.yml --syntax-check` |
| `hermes-openclaw-evidence.yml` | `hermes_hosts` | Inventory every preserved OpenClaw path, back up managed state, project the untouched source through a generated secret-redaction overlay and strictly read-only Astra-only bindfs view, prove exact native/bootstrap/reference parity, make Astra's Gateway require it, and periodically detect source/view drift; disabled without exact approval. | `ansible-playbook playbooks/agents/hermes-openclaw-evidence.yml --syntax-check` |
| `hermes-profile-memory.yml` | `hermes_hosts` | Transactionally stage four vault-encrypted, compact native memory seeds for Astra and Rigel while keeping Dubble empty and every Gateway stopped; disabled by default. | `ansible-playbook playbooks/agents/hermes-profile-memory.yml --syntax-check` |
| `hermes-profile-skills.yml` | `hermes_hosts` | Audit the retained root-owned 39-skill projection without mutation, or explicitly restore it from the 37 reviewed native sources as a rollback transaction; the default-off native ownership gate is separate and must not be enabled before an attended, off-host-backed import into profile-local roots. | `ansible-playbook playbooks/agents/hermes-profile-skills.yml --syntax-check` |
| `hermes-profile-data.yml` | `hermes_hosts` | Transactionally copy only reviewed project data and operator references, including Rigel's complete course, memory, syllabus, and inbound trees, into isolated per-profile writable/read-only roots; prove source and manifest stability plus runtime bind modes, and leave every Gateway stopped; disabled by default. | `ansible-playbook playbooks/agents/hermes-profile-data.yml --syntax-check` |
| `hermes-profile-transforms.yml` | `hermes_hosts` | Transactionally normalize five reviewed Astra/Dubble legacy state sources into isolated canonical per-profile roots, prove source/output stability and runtime bind modes, and leave every Gateway stopped; Rigel uses native writable academic state instead of a lossy transform. | `ansible-playbook playbooks/agents/hermes-profile-transforms.yml --syntax-check` |
| `openclaw.yml` | `openclaw_hosts` | Deploy OpenClaw AI agent. | `ansible-playbook playbooks/agents/openclaw.yml --syntax-check` |
| `hermes-health-receiver.yml` | `hermes_hosts` | Stage or cut over the Hermes-native isolated Health receiver and aggregate-only publisher; disabled by default. | `ansible-playbook playbooks/agents/hermes-health-receiver.yml --syntax-check` |
| `openclaw-isolated-gateway.yml` | `openclaw_hosts` | Stage a modernized split Gateway/Codex canary with immutable runtime/plugin code, separate no-login identities and secrets, fresh executor OAuth, and a required model proof; disabled by default. | `ansible-playbook playbooks/agents/openclaw-isolated-gateway.yml --syntax-check` |
| `openclaw-state-rehearsal.yml` | `openclaw_hosts` | Rehearse deterministic relocation of active file-backed session stores, quarantine copied delivery-recovery intent, and retain only the selected plus immediate rollback generations; disabled by default. | `ansible-playbook playbooks/agents/openclaw-state-rehearsal.yml --syntax-check` |
| `openclaw-doctor-rehearsal.yml` | `openclaw_hosts` | Rehearse credential-free supported state migrations and plugin modernization on protected copies with bounded upstream/Doctor generation retention; disabled by default. | `ansible-playbook playbooks/agents/openclaw-doctor-rehearsal.yml --syntax-check` |
| `openclaw-canary-data-rehearsal.yml` | `openclaw_hosts` | Transactionally hand the classified modern workspace and verified file-backed sessions to the loopback-only, channel/cron/heartbeat-suppressed five-agent canary, then plan or apply native session archival with rollback; disabled by default. | `ansible-playbook playbooks/agents/openclaw-canary-data-rehearsal.yml --syntax-check` |
| `openclaw-behavior-rehearsal.yml` | `openclaw_hosts` | Prove Dubble response discipline, native Vega/Antares Star lineage, and idle-silent Rigel heartbeat behavior in the channel-less loopback canary, then archive only the synthetic probe sessions; disabled by default. | `ansible-playbook playbooks/agents/openclaw-behavior-rehearsal.yml --syntax-check` |
| `openclaw-security-rehearsal.yml` | `openclaw_hosts` | Run one channel-less hostile-prompt probe against the split Gateway/executor and prove sudo, Gateway-secret, Docker, and outside-workspace denial from trajectory and filesystem evidence; disabled by default. | `ansible-playbook playbooks/agents/openclaw-security-rehearsal.yml --syntax-check` |
