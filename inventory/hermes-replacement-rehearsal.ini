# Dedicated inventory for the disposable Hermes replacement-node rehearsal.
# It deliberately contains no production host. The orchestration play adds the
# temporary Podman target to hermes_hosts only after exact approval.
[rehearsal_controller]
localhost ansible_connection=local

[hermes_hosts]
