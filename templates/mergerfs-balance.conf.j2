# mergerfs-balance default path excludes
# Managed by Ansible - playbooks/mergerfs.yml
# One fnmatch pattern per line. These are merged with any -E flags on the CLI.
{% for path in mergerfs_balance_exclude_paths %}
{{ path }}
{% endfor %}
